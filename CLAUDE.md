# 오디픽 (ODIPICK)

대전 지역 **상황 기반 AI 맛집 추천** 웹서비스.
"10명 회식, 룸 있는 고깃집" 처럼 조건을 넣으면 맞는 곳을 골라준다.

- 저장소: https://github.com/soyeonna/odipick
- 배포: Netlify (`netlify.toml` → `python3 scripts/build_dist.py` 로 `dist/` 생성)

---

## 함께 일하는 사람

**나소연** — 인스타그램 `@_princesspick_` 운영, 활동명 **"대전공주"** (팔로워 2.6만, 대전광역시 공식 홍보대사).
대전 맛집·카페·데이트 코스를 릴스로 소개한다. 팔로워를 **"공주들"** 이라 부르고, 채널 정체성은
*"공주들이 좋아할 만한 곳만 골라서 추천해준다"* 이다. 오디픽은 그 정체성을 웹으로 옮긴 서비스다.

**개발 비전문가다.** 코딩을 직접 하지 않는다.

---

## 응답 규칙 (반드시 지킬 것)

1. **한국어로, 전문용어 없이** 답한다. 어려운 말을 써야 하면 바로 옆에 쉬운 설명을 붙인다.
2. **코딩 화면을 보여주지 않는다.** 코드 블록, 터미널 출력, 파일 크기, 검증 로그 등은 응답에 넣지 않는다.
   → "무엇이 바뀌었고, 소연님이 뭘 하면 되는지"만 전한다.
   단, **소연님이 직접 복사해서 실행할 명령어**는 예외로 보여준다.
3. **되돌리기 어려운 일은 먼저 물어본다.** 파일 삭제·덮어쓰기·이동, 외부 공개, 배포 등.
   되돌릴 수 있는 작업은 묻지 말고 바로 한다.
4. **이 폴더(`odipick`) 밖은 건드리지 않는다.** 읽는 것도 포함. 필요하면 먼저 허락을 받는다.
5. **답변은 짧게.** 긴 분석은 파일로 저장하고 대화에는 결론만 남긴다.
   반복 작업은 스크립트로 만들어 소연님이 직접 실행하게 한다 (대화 비용이 안 들도록).
6. 한 번 정한 지침은 대화가 길어져도 계속 지킨다. 소연님은 지침이 흐지부지되는 걸 매우 싫어한다.

---

## 폴더 안내

| 파일 / 폴더 | 무엇인지 |
|---|---|
| `index.html` | **메인 사이트.** 검색과 추천이 여기서 다 일어난다 |
| `admin.html` | 맛집 데이터를 손으로 입력하는 화면 |
| `cardmaker.html` | 인스타에 올릴 카드 이미지 만드는 도구 |
| `report.html` | 서비스 점검 리포트 |
| `audit.html` | 맛집 분류가 맞는지 검수하는 표 |
| `kakao-guide.html` | 카카오 연동 안내 |
| `data/reels_parsed.json` | **릴스에서 뽑아낸 맛집 정보 222건** (가게명·지역·영업시간·좌표·가격 등) |
| `data/audit.json` | 분류 검수 대상 목록 |
| `data/reels_all.json` | 릴스 원본. 용량이 커서 저장소에 안 올림 → 새 컴퓨터에선 동기화 스크립트로 다시 받는다 |
| `scripts/` | 자동화 도구 모음 |
| `dist/` | 실제 서버에 올라가는 결과물. **직접 고치지 말 것** (빌드하면 덮어써짐) |
| `docs/` | 인스타·카카오 설정 안내서 |

### scripts 폴더

- `sync-reels.mjs` / `sync_reels.py` — 인스타 릴스를 가져와 맛집 데이터로 변환
- `read-reel.mjs` — 릴스 주소만으로 캡션 읽기 (로그인·토큰 불필요)
- `refresh-token.mjs` / `refresh_token.py` — 인스타 토큰 60일 연장. **만료 전에 돌려야 함**
- `check-closed.mjs` — 폐업·정보변경 감지
- `build_dist.py` / `build-deploy.mjs` — 배포용 `dist/` 만들기

---

## 반드시 알아야 할 규칙

**👑공주픽 뱃지는 대전공주 릴스에 실제로 나온 곳에만 붙인다.**
그 외에는 '로컬 아카이브'로 따로 둔다. **절대 섞지 말 것.** 채널 신뢰도와 직결된다.

**서비스 이름은 "오디픽 / ODIPICK" 으로 확정됐다.**
예전 이름인 PLACEPICK·플레이스픽·플픽은 다른 회사 서비스와 겹쳐서 폐기했다. 옛 이름이 코드에 남아 있으면 정리 대상.

**비밀 키는 `.env` 파일에 있고 저장소에 안 올라간다.**
`KAKAO_REST_KEY`, `IG_TOKEN`, `GITHUB_TOKEN` 세 가지. 키 원문을 대화에 붙여넣지 않는다.

**맛집 데이터는 공공데이터(localdata.go.kr) 인허가 정보를 쓴다.**
다이닝코드 같은 곳에서 긁어오지 않는다. 인허가일자로 노포·신상 구분도 가능하다.

---

## 컴퓨터 두 대로 작업할 때

소연님은 맥북과 다른 컴퓨터를 번갈아 쓴다.

- **작업 시작할 때** — 최신 내용 받아오기
- **작업 끝낼 때** — 저장하고 올려두기

소연님이 "시작하자" / "저장해줘" 라고 하면 알아서 처리한다.
**대화 내용은 컴퓨터를 넘어가지 않으므로**, 상황 파악은 이 문서와 커밋 기록을 읽어서 한다.

새 컴퓨터에서 처음 받았다면 `.env` 파일이 없다. 소연님께 직접 만들어달라고 안내한다.

---

## 다음에 할 일 (소연님이 미뤄둔 것 — 내가 먼저 꺼낼 것)

- **데이트코스 기능** — 좌표가 이미 있으니 같은 동네에서 카페 → 밥 → 술/디저트 를 동선으로 묶기. 데이터 확충 끝나면 착수
- **인스타 프로필 소개글 개편** — "공주 취향만 골라 담는 큐레이터" 방향으로 정리 중. 바꾼 뒤 프로필 방문 전환율 보고 다시 손보기
- **오디픽 링크를 인스타 프로필에 추가** — 배포 완료 후
- `.github` 폴더가 저장소에서 빠져 있음. 자동 배포를 쓸 거면 손봐야 함
