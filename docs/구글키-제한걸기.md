# 구글 키에 잠금 걸기 (10분)

## 왜 해야 하나
오디픽 사이트 안에는 구글 지도 키가 들어 있다. 이건 원래 공개되는 종류라 문제가 아니다.
**문제는 지금 아무 제한이 없다는 것이다.**

2026-09-10 확인: 남의 사이트인 척 요청해도 그대로 통과했다.
즉 누구나 사이트 소스에서 키를 복사해 자기 서비스에 쓸 수 있고, **요금은 소연님 카드로 나간다.**

잠금을 걸면 **오디픽 주소에서 온 요청만** 통과한다.

---

## 하는 법

### 1단계 — 구글 클라우드 콘솔 열기
https://console.cloud.google.com/apis/credentials 로 들어간다.
소연님 구글 계정(nasoyeon6@gmail.com)으로 로그인.

### 2단계 — 키 찾기
"API 키" 목록에 **`Maps Platform API Key`** 하나가 있다. **파란 글씨로 된 이름을 클릭**한다.
(목록 화면에서는 제한을 걸 수 없다. 반드시 이름을 눌러 상세 화면으로 들어가야 한다.)

지금 "제한사항" 칸에 **`API 2개`** 라고 되어 있는데, 이건 *쓸 수 있는 기능*만 막아둔 것이고
**주소 제한은 아직 없다.** 그래서 누구나 키를 가져다 쓸 수 있다.

### 3단계 — 주소 제한 걸기
상세 화면을 아래로 내리면 **"애플리케이션 제한사항"** 이 있다.
기본값이 **`없음`** 으로 되어 있을 것이다. 이걸 **`웹사이트`** 로 바꾼다.
그러면 아래에 **"웹사이트 제한사항"** 칸이 생긴다. **"항목 추가"** 를 눌러 아래 줄을 넣는다.

```
https://soyeonna.github.io/*
```
```
https://odipick.netlify.app/*
```

두 번째 줄은 넷리파이를 다시 쓸 때를 대비한 것이다. 안 넣어도 된다.

### 4단계 — 쓸 수 있는 기능 확인
조금 더 내리면 **"API 제한사항"** 이 있다. 이미 `키 제한`으로 2개가 걸려 있을 텐데,
아래 두 개가 맞는지 확인한다.

- **Places API (New)**
- **Street View Static API**

나머지는 체크 해제. 오디픽은 이 둘만 쓴다.

### 5단계 — 저장
아래 **"저장"** 을 누른다. **적용까지 5분쯤 걸린다.**

---

## 걸고 나서 확인
저장하고 10분 뒤, 사이트에서 아무 매장이나 눌러 **사진이 잘 나오는지** 보면 된다.
사진이 안 나오면 3단계 주소를 잘못 적은 것이니 다시 확인한다.

매일 새벽 자동 작업은 이미 "오디픽에서 왔다"고 알리도록 되어 있어서 **잠금을 걸어도 그대로 돌아간다.**

---

## 무료 체험판 (2026-09-10 확인)
크레딧 **40만 7천원**이 남아 있고 **83일** 남았다. 대략 **2026년 12월 초**에 끝난다.

- 체험판이 끝나도 **자동으로 요금이 청구되지 않는다.** 직접 유료로 바꿔야 계속 쓸 수 있다.
- 사진을 이미 오디픽 안에 받아뒀기 때문에 **체험판이 끝나도 사이트 사진은 그대로 나온다.**
- 다만 매일 새벽 하는 구글 정보 갱신(영업시간·평점·사진)은 멈춘다. 그때 다시 정하면 된다.
- 40만원은 거의 안 쓸 것이다. 지금 쓰는 양이 한 달에 몇 천원 수준이다.

## 예산 알림도 같이 걸어두기 (5분, 권장)
https://console.cloud.google.com/billing → **예산 및 알림** → **예산 만들기**
금액을 **1만원**쯤으로 잡아두면, 그 이상 나올 때 메일이 온다.
지금은 구글 사진을 오디픽 안에 받아둬서 요금이 거의 안 나가지만, 안전장치로 두는 게 좋다.
